<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <% if user_signed_in? %>
    <% if current_user.is_admin %>
      <strong><%= link_to "CatStore", admin_items_path, :class => "navbar-brand" %></strong> 
    <% else %>
      <strong><%= link_to "CatStore", root_path, :class => "navbar-brand" %></strong> 
    <% end %>
  <% else %>
    <strong><%= link_to "CatStore", root_path, :class => "navbar-brand" %></strong> 
  <%end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">
    
    </ul>
          <ul class="navbar-nav">
            <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to "<i class='fas fa-shopping-cart fa-lg '></i> <span class='badge badge-pill badge-danger'>#{current_user.cart.items.count}</span>".html_safe, cart_path(current_user.id), class:"nav-link btn btn-outline-secondary mr-2"%>
            </li>
            <% end %>
          </ul>

          <ul class="navbar-nav border-nav">
          <% if user_signed_in? %>
            <li class="nav-item dropdown">
              <button type="button" class="btn btn-outline-secondary dropdown-connect dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" id="navbarDropdown1">
                <%= current_user.email %>
              </button>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown1">
                <%= link_to "Profil", user_path(current_user.id), class: "dropdown-item" %>
                <% if current_user.is_admin %>
                  <%= link_to "Dashboard Admin", admin_dashboard_index_path, class: "dropdown-item" %>
                <% end %>
                <div class="dropdown-divider"></div>
                <%= link_to "Se déconnecter", destroy_user_session_path, class: "dropdown-item", method: :delete %>
              </div>
              </li>
          <% else %>
            <li class="btn-group">
              <button type="button" class="btn btn-secondary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" id="navbarDropdown2">
                Se connecter
              </button>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown2">
                <%= link_to "S'inscrire", new_user_registration_path, class:"dropdown-item" %>
                <div class="dropdown-divider"></div>
                <%= link_to "Se connecter", new_user_session_path, class:"dropdown-item" %>
              </div>
            </li>
          <% end %>
        </ul>
      </div>
  </div>
</nav>
